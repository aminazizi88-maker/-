# [نام پروژه]

## نوع پروژه
پروژه وب — [توضیح یک خطی: مثلاً "داشبورد مدیریت فروش"]

## فایل‌های اصلی
- `index.html` — صفحه اصلی
- `style.css` — استایل‌ها
- `app.js` — منطق اصلی
<!-- فایل‌های دیگر را اینجا اضافه کن -->

## تکنولوژی
- زبان: [Vanilla JS / React / Vue / ...]
- CSS: [Pure CSS / Tailwind / Bootstrap / ...]
- Backend: [ندارد / Node.js / PHP / ...]
- دیتابیس: [ندارد / localStorage / Firebase / ...]

## معماری
- [مثال: state اصلی در متغیر `appState`]
- [مثال: رویدادها با `addEventListener` مدیریت می‌شوند]
- [مثال: داده در localStorage با کلید `myApp` ذخیره می‌شود]

## قوانین کار
- بدون build tool / npm مگر اینکه پروژه داشته باشد
- بدون کامنت اضافه در کد
- فقط بخش مرتبط را بخوان، نه کل فایل
- سازنده: امین عزیزی مقدم
